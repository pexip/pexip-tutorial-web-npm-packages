// TODO (30) Import all the needed dependencies
import { Modal } from '@pexip/components'

import './SettingsModal.css'

// TODO (31) Define SettingsModal props

// TODO (32) Include props in SettingsModal function attributes
export const SettingsModal = (): JSX.Element => {
  // TODO (33) Add localStream state

  // TODO (34) Add audioInput, audioOutput and videoInput states

  // TODO (35) Add handleVideoInputChange function

  // TODO (36) Add handleSave function

  // TODO (37) Add useEffect when props.isOpen changes

  // TODO (38) Modify return statement to include header, video, device selectors and save button
  return <Modal isOpen={true} />
}
